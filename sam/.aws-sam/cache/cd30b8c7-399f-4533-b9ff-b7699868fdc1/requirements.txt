# Core LangChain and LangGraph dependencies (updated for production readiness)
# Using flexible constraints to allow pip to resolve compatible versions
langchain>=0.3.13,<0.4.0
langchain-core>=0.3.26,<0.4.0
langchain-community>=0.3.13,<0.4.0
langgraph>=0.2.50,<0.3.0

# AWS Bedrock support (production)
langchain-aws>=0.2.6,<0.3.0

# AWS SDK
boto3>=1.35.0
botocore>=1.35.0

# NOTE: OpenAI/LM Studio support removed for production
# These add 15MB+ and aren't needed for Bedrock-only deployment
# langchain-openai>=0.2.8,<0.3.0  # Only for local dev
# tiktoken>=0.8.0                  # Only needed by OpenAI

# Document processing
langchain-text-splitters>=0.3.2,<0.4.0

# NOTE: FAISS removed - using DynamoDB for vector storage
# This eliminates 160MB of dependencies (faiss-cpu + numpy)

# Pydantic for validation (LangChain 0.3.x uses Pydantic v2)
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0

# For production deployment - compatible with AWS Lambda Python 3.11+ runtime
# Note: Flexible version constraints for pip to resolve compatible versions

# Pin numpy to version compatible with SAM build container (GCC 7.3)
numpy>=1.24.0,<2.0.0
